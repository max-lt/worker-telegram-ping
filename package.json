{
  "private": false,
  "name": "worker-telegram-ping",
  "version": "1.0.0",
  "description": "ServiceWorker to monitor websites",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch | wrangler dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "max-lt <https://github.com/max-lt>",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.3.0",
    "prettier": "^1.19.1",
    "typescript": "^4.5.4"
  }
}
